# Database
DATABASE_URL="postgresql://prompteasy:your_password@localhost:5432/prompteasy"

# Redis (optional - for caching and rate limiting)
REDIS_URL="redis://:your_password@localhost:6379"

# ============================================================================
# VERCEL DEPLOYMENT SETUP
# ============================================================================
# For Vercel deployments, you MUST set these environment variables in your
# Vercel project settings (Settings → Environment Variables):
#
# Required for ALL environments (Production, Preview, Development):
#   - NEXT_PUBLIC_APP_URL = https://your-production-domain.com
#   - NEXTAUTH_SECRET = (generate with: openssl rand -base64 32)
#   - GOOGLE_CLIENT_ID = (from Google Cloud Console)
#   - GOOGLE_CLIENT_SECRET = (from Google Cloud Console)
#   - DATABASE_URL = (your production database URL)
#
# Required for Production only:
#   - NEXTAUTH_URL = https://your-production-domain.com
#
# This setup allows Google OAuth to work on preview deployments by redirecting
# users to production for authentication, then back to the preview deployment.
# See VERCEL_PREVIEW_OAUTH_SETUP.md for complete instructions.
# ============================================================================

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# Public App URL - REQUIRED for Vercel preview deployment OAuth
# Local development: http://localhost:3000
# Vercel: Set to your production URL for ALL environments (Production, Preview, Development)
#         Example: https://prompteasy.ndsrf.com
# This allows preview deployments to redirect to production for OAuth, then redirect back
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Google OAuth
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# OpenAI API (for AI-powered prompt improvement)
OPENAI_API_KEY="your-openai-api-key"

# Anthropic API (for AI-powered prompt improvement)
ANTHROPIC_API_KEY="your-anthropic-api-key"

# Okta OAuth (Enterprise tier - optional)
OKTA_CLIENT_ID="your-okta-client-id"
OKTA_CLIENT_SECRET="your-okta-client-secret"
OKTA_ISSUER="https://your-okta-domain.okta.com"

# Stripe (for billing and subscriptions - optional)
STRIPE_PUBLIC_KEY="your-stripe-public-key"
STRIPE_SECRET_KEY="your-stripe-secret-key"
STRIPE_WEBHOOK_SECRET="your-stripe-webhook-secret"
STRIPE_PRICE_ID_PRO_MONTHLY="price_xxx"
STRIPE_PRICE_ID_PRO_YEARLY="price_xxx"
STRIPE_PRICE_ID_ENTERPRISE_MONTHLY="price_xxx"
STRIPE_PRICE_ID_ENTERPRISE_YEARLY="price_xxx"

# App Settings
NODE_ENV="development"
PORT=3000
